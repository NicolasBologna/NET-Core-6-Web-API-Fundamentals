/*PAra usar esto hay q cambiarlo desde project -> properties -> debug(general) abrir la ventana y cambiarlo en environment variables.*/
{
  "mailSettings": {
    "mailToAdress": "admin@company.com"
  }
}
